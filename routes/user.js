const express = require('express');
const router = express.Router();

// Placeholder for user routes
router.get('/profile', (req, res) => {
  res.send('User profile endpoint active');
});

module.exports = router;